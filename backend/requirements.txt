# Versi dipatok persis, bukan `>=`.
#
# GitHub Actions dan build Hugging Face menjalankan `pip install` yang segar tiap
# kali, jadi rentang terbuka berarti versi bisa melompat semalam tanpa satu baris
# kode pun berubah — dan kegagalannya muncul di batch harian, jauh dari mata.
# 21 Juli 2026 OHLC 605 saham tertulis NaN dalam satu run; dengan rentang
# terbuka, run semacam itu bahkan tak bisa direproduksi untuk diperiksa.
#
# Menaikkan versi: ubah di sini, jalankan `pytest tests/ -q`, baru commit.

fastapi==0.136.1
uvicorn[standard]==0.46.0
sqlalchemy==2.0.49
python-dotenv==1.2.2
yfinance==1.3.0
pandas==3.0.2
ta==0.11.0
numpy==2.4.4
pytest==9.0.3
pytest-mock==3.15.1
httpx==0.28.1
mcp==1.27.0
groq==1.5.0
PyJWT==2.12.1
psycopg2-binary==2.9.12
curl_cffi==0.15.0
google-generativeai==0.8.6
